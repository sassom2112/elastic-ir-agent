# Core — works on Python 3.8+
elasticsearch>=8.0.0
python-dotenv>=1.0.0
pyyaml>=6.0
requests>=2.31.0
evtx>=0.8.0

# Local agent — uses Gemini REST API directly (no SDK needed, Python 3.8 compatible)
# No extra install required — only 'requests' above

# GCP Agent Engine deployment scripts — require Python 3.10+
# Agent Engine is the GA managed runtime under Vertex AI Agent Builder.
# Install separately in a Python 3.10+ environment:
#   pip install "google-cloud-aiplatform[agent_engines]>=1.87.0"
